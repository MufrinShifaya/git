name: Demo Pipeline

on: push

jobs:
  my-test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "CI Pipeline Running"

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Run Python file
        run: python app.py
